<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net6.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <Version>0.1.0</Version>
        <AssemblyVersion>0.1.0</AssemblyVersion>
        <releaseNotes>Added package meta data</releaseNotes>
        <tags>retry utility task async backoff</tags>
        <license type="expression">MIT</license>
        <icon>icon.png</icon>
        <description>Task Endurer is a library that lets you easily retry asynchronous tasks based on a given policy.</description>
    </PropertyGroup>

    <ItemGroup>
        <AssemblyAttribute Include="System.Runtime.CompilerServices.InternalsVisibleToAttribute">
            <_Parameter1>TaskEndurer.Tests</_Parameter1>
        </AssemblyAttribute>
    </ItemGroup>

</Project>
